import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// ReactDOM으로 render 하겠다.
// App 컴포넌트를
// id가 root인 엘리먼트에
ReactDOM.render(<App />, document.getElementById("root"));
